
#Aside
  = debug @children
  = raw module_to(@modules, 'left')

#MainCol
  .text.clearFix
    = raw module_to(@modules, 'top')
    %h1= @content.title
    %p= raw @content.body
    %p= @page.parent_path
    = raw module_to(@modules, 'partners')
